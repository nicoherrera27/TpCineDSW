enum RolUsuario {
  usuario
  admin
}

model Usuario {
  Id          Int        @id @default(autoincrement())
  Email       String     @unique
  Contrasenia String
  Nombre      String
  Apellido    String
  Rol         RolUsuario @default(usuario)
}
